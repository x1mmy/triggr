# Dependencies & build (matches anywhere in tree)
node_modules
dist
.DS_Store
*.local

# Env files
.env
landing-page/.env

# Design handoff (local only)
_design-handoff/

# Local marketing and mockup assets (not part of the deployed site)
marketing/
mockups/
